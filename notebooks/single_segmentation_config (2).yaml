data_dir: /home/ace/Downloads/Dataset
save_dir: ./single_segmentation_outputs
device: cuda

selection:
  # Option A: exact filename
  image_name: null

  # Option B: select by location id and condition (Example: 878_Day_EXTRASUNNY)
  location_id: "878"
  illumination: Day
  weather: EXTRASUNNY
  pick_index: 0

model:
  name: SegFormer-B5
  type: segformer
  model_id: nvidia/segformer-b5-finetuned-cityscapes-1024-1024

# Alternative example:
# model:
#   name: Mask2Former-Swin-L
#   type: mask2former
#   model_id: facebook/mask2former-swin-large-cityscapes-semantic

export:
  preview_figsize: [12, 5]
  preview_dpi: 200
  save_png_too: true
  save_raw_prediction_npy: true
  save_confidence_npy: false
